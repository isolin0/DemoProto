using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemInfoTrigger : MonoBehaviour {

    //public ItemDataBase.Item itemInfo;
    public SlotInfo slotInfo;
    


    public void TriggerDescription()
    {
        //  FindObjectOfType < ItemInfoManager>().StartDescription(slotInfo);
        //Debug.Log("Item info " + slotinfo.id + slotinfo.itemId + slotinfo.isEmpty);
    }
}
